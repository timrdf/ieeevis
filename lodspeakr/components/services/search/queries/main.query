prefix vsr: <http://purl.org/twc/vocab/vsr#>

select distinct ?g where { 
  GRAPH ?g { 
    ?g a vsr:Dataset. 
    ?some rdfs:label ?string .
    filter(regex(?string,"{{lodspk.args.arg0}}","i"))
  }
}
